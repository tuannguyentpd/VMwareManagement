#pragma once

#include <vix.h>

class VirtualMachine
{
private:
    VixError err = VIX_OK;
    VixHandle jobHandle = VIX_INVALID_HANDLE;
    VixHandle vmHandle = VIX_INVALID_HANDLE;

public:
    
};

